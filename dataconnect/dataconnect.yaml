specVersion: "v1beta"
serviceId: "agenda_laps"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "agenda_laps-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
